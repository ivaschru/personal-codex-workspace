# Сервисные навыки

Все интеграции поставляются вместе с репозиторием и настраиваются по первому запросу. Они не содержат аккаунтов, cookies, токенов, персональных идентификаторов и браузерных профилей автора шаблона.

## Проверка

Показать доступные интеграции:

```bash
python3 scripts/integration_preflight.py --list
```

Проверить конкретную интеграцию:

```bash
python3 scripts/integration_preflight.py ozon-buyer-search
```

Код `0` означает готовую настройку, `2` – незавершённую первичную настройку репозитория, `3` – интеграция доступна, но ещё не включена владельцем.

## Общие границы

- Первый вход и проверка личности выполняются владельцем.
- CAPTCHA, SMS, push, TOTP, электронная подпись и восстановление доступа не автоматизируются.
- Навык может читать только выбранный пользователем кабинет, чат, заказ, обращение или диапазон операций.
- Финальная отправка, платёж, подпись или изменение аккаунта требуют отдельного разрешения либо остаются ручным действием по правилам конкретного навыка.
- Секреты не записываются в `workspace.json`, `PROFILE.md`, task logs или Git.

## Доступные интеграции

| Навык | Возможности из коробки | Обязательное участие владельца |
| --- | --- | --- |
| `ozon-buyer-search` | поиск, сравнение, карточки, отзывы, чтение выбранного собственного заказа | вход для личных заказов, CAPTCHA |
| `max-messenger` | чтение выбранного чата, вложения, подготовленная отправка | первый вход, подтверждение отправки |
| `telegram-messenger` | поиск и чтение выбранного чата, вложения, подготовленная отправка | первый вход/QR, подтверждение отправки |
| `gosuslugi` | вход через ЕСИА, чтение выбранной услуги/статуса, подготовка черновика | вход, подтверждение личности, подпись и финальная отправка |
| `gas-pravosudie` | чтение обращений и истории, подготовка комплекта | ЕСИА, электронная подпись и финальная подача |
| `russian-post-registered-mail` | подготовка, проверка адресата и комплекта, получение доказательств | ЕСИА, оплата и финальная отправка |
| `t-bank` | чтение выбранного баланса, операций или выписки | вход, CAPTCHA, SMS/push; все изменения запрещены |
| `trelio` | проекты, карточки и контекст; подтверждённые комментарии и обновления | OAuth/вход, явное разрешение на каждую мутацию |

## Telegram: браузер и MTProto

Telegram Web не требует ключей приложения и является базовым режимом. Для более точного поиска, работы с большими диапазонами сообщений и вложениями можно подключить отдельный MTProto-инструмент.

В MTProto-режиме каждый владелец создаёт собственное приложение на `my.telegram.org` и хранит `TELEGRAM_API_ID` и `TELEGRAM_API_HASH` только в ignored `.local/telegram/app.env` или другом локальном защищённом хранилище. Публичный шаблон не содержит общих или авторских ключей приложения, телефона, session-файла и login-кодов.

## Trelio: только MCP

Trelio подключается только через MCP endpoint `https://trelio.ru/mcp` и штатный OAuth. Браузерный доступ к `trelio.ru` не используется как агентский fallback. Если callable MCP tools отсутствуют, нужно зарегистрировать MCP-сервер поддерживаемым текущей поверхностью Codex способом, пройти OAuth и открыть новую сессию Codex.

OAuth-токены и callback URL с одноразовыми кодами хранятся только в локальном MCP-клиенте и не попадают в репозиторий.

## Т-Банк на macOS и Windows

Базовый переносимый режим использует отдельный браузерный профиль или уже авторизованную сессию, защищённую учётной записью ОС. На macOS рекомендуется FileVault, на Windows – BitLocker. Windows Credential Manager и macOS Keychain могут хранить только настройки, которые владелец осознанно решил сохранить; публичный навык сам не сохраняет пароль или TOTP.

Если шифрование диска не подтверждено, навык может работать только в ручной интерактивной сессии без отдельного сохранения банковских секретов.
